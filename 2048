  1、向左移动需要的条件：
    （1）是否可以左移？
    循环棋盘的所有的方格，第一列除外；
    检查元素，如果当前的元素是data数据里面的初始数据‘0’，表示没有元素需要左移，那么没必要检查；
    如果当前元素不是初始数据0，而是更新在棋盘上面的数据，那么才有必要检查；
    检查的条件：当前元素的左侧元素是初始数据‘0’，是空的地方，或者当前元素的左侧数据等于当前元素
    关于代码的两个return；return返回值都是当前的函数，如果左侧元素为空或者左侧元素等于当前元素，那么返回true，
    moveLeft:function(){
    if(this.canLeft()){}这个方法可以执行；如果不满足左侧元素为空或者左侧元素等于当前元素这个条件，那么返回false，
    moveLeft:function(){
    if(this.canLeft()){}这个方法就无法执行

    （2）找右侧下一个不为0的数
    从row行地col位置之后开始，遍历row行的剩余元素，如果这个元素不等于初始数据0，那么返回这个数据
    如果等于0，那么返回-1
    （3）左移一行，要移动的行下标
    从0开始，遍历第row行的每个元素
    获得当前位置后，下一个不为零的数的位置
    如果下一个位置的数的位置=-1，表明下一个不为零的数是没有位置的，推出循环
    如果下一个位置的数的位置！=-1，表明有空间，可以进行移动，有两种情况：
    A、如果当前位置为0，将下一个位置的数放在当前位置，下一个位置的数为0；
    B、如果当前位置的数和下一个位置的数相同，那么将当前位置的数*2，下一个位置的数为0；

 2、向右移动需要的条件：
   （1）是否可以右移？
    循环棋盘的所有的方格，最右一列除外；
    检查元素，如果当前的元素是data数据里面的初始数据‘0’，表示没有元素需要右移，那么没必要检查；
    如果当前元素不是初始数据0，而是更新在棋盘上面的数据，那么才有必要检查；
    检查的条件：当前元素的右侧元素是初始数据‘0’，是空的地方，或者当前元素的右侧数据等于当前元素
    关于代码的两个return；return返回值都是当前的函数，如果右侧元素为空或者右侧元素等于当前元素，那么返回true，
    moveRight:function(){
    if(this.canRight()){}这个方法可以执行；如果不满足右侧元素为空或者右侧元素等于当前元素这个条件，那么返回false，
    moveRight:function(){
    if(this.canRight()){}这个方法就无法执行
    （2）右移一行，要移动的行下标
        从0开始，遍历第row行的每个元素
        获得当前位置后，下一个不为零的数的位置
        如果下一个位置的数的位置=-1，表明下一个不为零的数是没有位置的，退出循环
        如果下一个位置的数的位置！=-1，表明有空间，可以进行移动，有两种情况：
        A、如果当前位置为0，将下一个位置的数放在当前位置，下一个位置的数为0；
        B、如果当前位置的数和下一个位置的数相同，那么将当前位置的数*2，下一个位置的数为0；
    （3）找右侧下一个不为0的数
        从row行地col位置之后开始，遍历row行的剩余元素，
        如果这个元素不等于初始数据0，那么返回这个数据
        如果等于0，那么返回-1

 3、向上移动需要的条件：
    可以移动的话，循环数据的每一列，循环体里调用moveUpInCol方法；
    循环体外调用一次随机生成数字，移动一次生成1个数字；然后再调用一次将数字更新到棋盘上；
    （1）是否可以上移？
     循环棋盘的所有的方格，最上面的一行除外；
     检查元素，如果当前的元素是data数据里面的初始数据‘0’，表示没有元素需要右移，那么没必要检查；
     如果当前元素不是初始数据0，而是更新在棋盘上面的数据，那么才有必要检查；
     检查的条件：当前元素的上方元素是初始数据‘0’，是空的地方，或者当前元素的上方数据等于当前元素
     关于代码的两个return；return返回值都是当前的函数，如果右侧元素为空或者右侧元素等于当前元素，那么返回true，
     moveUp:function(){
     if(this.canUp()){}这个方法可以执行；如果不满足右侧元素为空或者右侧元素等于当前元素这个条件，那么返回false，
     moveUp:function(){
     if(this.canUp()){}这个方法就无法执行
     （2）上移一行，要移动的列下标
         从0开始，遍历第col列的每个元素
         获得当前位置后，下一个不为零的数的位置
         如果下一个位置的数的位置=-1，表明下一个不为零的数是没有位置的，退出循环
         如果下一个位置的数的位置！=-1，表明有空间，可以进行移动，有两种情况：
         A、如果当前位置为0，将下一个位置的数放在当前位置，下一个位置的数为0；
         B、如果当前位置的数和下一个位置的数相同，那么将当前位置的数*2，下一个位置的数为0；
     （3）找下面下一个不为0的数
         从col列的第row行位置之后开始，遍历col列的剩余元素，
         如果这个元素不等于初始数据0，那么返回这个数据
         如果等于0，那么返回-1
